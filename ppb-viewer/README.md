## Document hierarchy viewer

Start a Pythons server from the root directory of this repo:

```bash
uv run python -m http.server
```

Then, open the viewer in your browser at http://localhost:8000/ppb-viewer/.
